# Build artifacts
projecthub-api/target/
projecthub-web/node_modules/
projecthub-web/dist/

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# Environment variables (if you create .env files later)
.env
.env.local

# Docker volumes (local data)
# Note: docker-compose.yml volumes are named, so they won't be committed anyway
